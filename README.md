Opatomic admin tool for the browser

Use this tool to monitor your server and run queries. It is included with the
Opatomic server. Requires a websocket proxy server running between the browser
and server.

